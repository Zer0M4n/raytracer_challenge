mod math;
mod color;
mod utils;
mod canvas;

use crate::color::Color;
use crate::math::point::Point;
use crate::math::vector::Vector;
use crate::canvas::Canvas;
fn main() {
    println!("Hello, world!");
}
